{
  "name": "project2",
  "version": "1.0.0",
  "description": "json server -->// there are some api's that we cannot customize \r               // while working in construct week project we need to use api but the api may not exists then we need to create fake data base with json server that we can access with api call.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
